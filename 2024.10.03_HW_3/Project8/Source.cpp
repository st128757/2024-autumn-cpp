#include<cstdio>

int main(int argc, char* argv)
{
	int i = 0;
	int j = 0;

	scanf_s("%d", &i);
	scanf_s("%d", &j);

	for (int d = 1; d < i && d < j; ++d)
	{
		if (i % d == 0 && j % d == 0)
		{

		}
	}

	return 0;
}